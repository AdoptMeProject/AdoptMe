

<article class="container">

  <div class="row">

    <div class="col-2">
      <img class="w-100 rounded-circle" src="{{user.avatar}}" />
    </div>

    <div class="col-10">

      <h1>{{user.name}}</h1>
      <p>{{user.bio}}</p>
      <p><strong>Email:</strong> {{user.email}}</p>

      {{#if user.shelter}}

        <p><strong><i class="fa fa-star mr-1"></i>SHELTER</strong></p>
        <p>City: {{user.city}}</p>
        <Address>{{user.address}}</Address>
        <p>Contact: {{user.phone}}</p>
        
      {{/if}}

      {{#ifEquals currentUser.email user.email}}

        <div>

          <a class="btn btn-secondary" href="/users/{{currentUser._id}}/edit">
            <i class="fa fa-edit mr-2"></i>
            Edit
          </a>

          <form action="/users/{{currentUser._id}}/delete" method="POST" class="mt-2">
            <button type="submit" class="btn btn-danger">
              <i class="fa fa-trash mr-2"></i>
              Remove account
            </button>
          </form>

        </div>

      {{/ifEquals}}

    </div>

  </div>

  <div>

    {{#if user.shelter}}

      <h2 class="my-4">Shelter posts</h2>
      
      <div class="row">
        {{#each user.shelterposts as |post|}}
        <div class="col-xl-4 col-lg-6 col-sm-12 mb-4">
          {{>posts/postcard post=post }}
        </div>
        {{/each}}
      </div>

      <hr/>

    {{/if}}

  </div>

  <div id="map"></div>

<iframe
  width="600"
  height="450"
  frameborder="0" style="border:0"
  src="https://www.google.com/maps/embed/v1/place?key=AIzaSyCP9mRiFIygvhTnPa7LOMo2clF-k2jINJ8&q={{user.locationTest}}" allowfullscreen>
  {{!-- src="https://www.google.com/maps/embed/v1/place?key=AIzaSyCP9mRiFIygvhTnPa7LOMo2clF-k2jINJ8&q=ALBA+Protectora,Madrid+Spain" allowfullscreen> --}}
</iframe>

  <div>

    <div class="row">

      {{#each user.posts as |post|}}
        <div class="col-xl-4 col-lg-6 col-sm-12 mb-4">
          {{>posts/postcard post=post }}
        </div>
      {{/each}}

    </div>

  </div>

</article>
{{user}}
<script>
  
  var userN=decodeURIComponent("{{{user.locationMap}}}");
  console.log(userN)
  var newUSer =JSON.parse(userN);
  console.log(`Hola ${newUser}`)
  window.user = newUser;
</script>
